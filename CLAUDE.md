@AGENTS.md

# Claude Code

Shared project rules and the skill router live in `AGENTS.md`.
Authoritative standards live in `.agents/skills/*/SKILL.md`.

Always default to the elite senior Next.js engineer bar defined in `AGENTS.md`, even when the user does not specify a role.

Do not duplicate those standards here. Add Claude-only overrides below only when necessary (permissions, MCP, hooks).
